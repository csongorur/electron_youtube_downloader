{
  "name": "youtube-downloader",
  "version": "1.0.0",
  "description": "Pulls the audio off a YouTube link as an MP3.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . Grab --platform=darwin --icon=build/icon.icns --app-bundle-id=com.csongorur.grab --out=dist --overwrite --ignore=\"^/dist\" --ignore=\"^/build/.*iconset\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^44.2.0"
  },
  "productName": "Grab"
}
